<% content_for :sidebar, label_sidebar(@label) %>
<% content_for :nav do %>
  <%= link_to 'Back', labels_path %>
<% end %>

<% content_for :page_title, "#{link_to 'Labels', labels_path} / #{@label.display_name}".html_safe %>

<%= list_attributes(@label, {'Institution' => label_source_name(@label)}, :id_tag, :inscription, :dimensions, :attachment_method, :attachment_location) %>

<%= render 'comments/commentable_comments', :commentable => @label %>

<%= javascript_tag "PopupGallery('#record_media .media_thumbnail')" %>