<% content_for :nav do %>
  <%= link_to 'Back', cards_path %>
<% end %>

<% content_for :page_title, "#{link_to 'Catalogue Cards', cards_path} / #{@card.to_s}".html_safe %>

<%= render @card.media_items %>

<%= list_attributes(@card, {'Institution' => card_source_name(@card)}, :id_tag, :inscription, :location) %>
<%= render 'comments/commentable_comments', :commentable => @card %>