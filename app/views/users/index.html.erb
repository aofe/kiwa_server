<table>
  <tr><th>User</th><th>Can download?</th></tr>
  <% @users.each do |user| %>
  <tr><td><%= user.username %> (<%= mail_to user.email %>)</td><td><%= user.can_download? %></td>
  <% end %>
</table>